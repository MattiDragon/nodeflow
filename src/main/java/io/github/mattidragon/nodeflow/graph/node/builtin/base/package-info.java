/**
 * Contains node classes that don't correspond to a single node type, but are instead used to create nodes with similar behaviour.
 */
package io.github.mattidragon.nodeflow.graph.node.builtin.base;